<header class="sticky-top">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div style="float:right">
            <a class="navbar-brand " href="/">TOP</a>
        </div>
        <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#Navber" aria-controls="Navber" aria-expanded="false" aria-label="ナビゲーションの切替">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="Navber">
            <ul class="navbar-nav mr-autov ">
                {% if session.user_info  =="" %}
                    <li class="nav-item">
                        <a class="nav-link" href="/login/">ログイン</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register/">会員登録</a>
                    </li>

                {% else %}
                    {# ここに↓メニュー追加していく #}
                    <li class="nav-item">
                        <a class="nav-link" href="/logout/">ログアウト</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/resign/">退会</a>
                    </li>


                {% endif %}
            </ul>
            {# ログインしてたらユーザー名を出力する#}
            {% if session.user_info.name %}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/profile_setting/">{{ session.user_info.name }}さん</a>
                    </li>
                </ul>
            {% endif %}
        </div>
        <!-- /.navbar-collapse -->
    </nav>
</header>
